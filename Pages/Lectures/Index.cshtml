@page
@model RazorPagesMovie.Pages_Lectures.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Lecture[0].Ten)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lecture[0].GVGD)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lecture[0].SoTiet)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Lecture) {
        <tr>
             <td>
                @Html.DisplayFor(modelItem => item.Ten)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GVGD)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SoTiet)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Ten">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Ten">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Ten">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
